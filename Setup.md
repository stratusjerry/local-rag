Setup Plan
===

Find a public repo of documents to index, include .docx, .ppt, .txt, and possible images (image to text conversion)


### LLM Prompts
`What public dataset will give me a variety of windows documents like .docx, .ppt, .txt?`

`Create a Python project that will run on a Windows machine and create a RAG database that ingests .docx, powerpoint and .txt files files from a local drive. A local GUI using Streamlit will allow the user to query the RAG database`
